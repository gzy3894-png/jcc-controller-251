# 任务计划（自定）

## 目标
原版 libJCC.so 逻辑不动，只补新赛季数据/符号，打回 Controller 大包。

## 阶段
- **P1** 符号对照 + 读数字段立即数证据链
- **P2** PATCH-TABLE + 自动 patch 脚本
- **P3** patched SO + 固定签名 APK + Release

## 不做
重写自动买/预测算法；转区；未验证就宣称完整。
